package com.qinghua.leetcode.medium;

public class Q6 {
    public String convert(String s, int numRows) {

        //初始化一个二维数组 numRows 行 s.length/numRows 列
        double v = (double) s.length() / (double) numRows;
        double ceil = Math.ceil(v);

        return "";

    }
}
